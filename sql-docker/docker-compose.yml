version: '3.8'

services:
  mysql:
    image: mysql:latest
    container_name: mysql-pbl-619
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: 123456
      MYSQL_DATABASE: db3_hydro
    ports:
      - "9898:3306"
    volumes:
      - mysql-data:/var/lib/mysql

volumes:
  mysql-data:
